function filterSearch(event) {
	const searchTerm = event.target.value;
	const searchResults = document.querySelector('.search-result');
	console.log(searchTerm);
}


// Get the search_term to submit to tabels and update it.
